name: 'supply_chain'
version: '1.0.0'
config-version: 2
profile: 'supply_chain'

model-paths: ["models"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  supply_chain:
    staging:
      +materialized: view
      +schema: silver
    marts:
      +materialized: table
      +schema: gold
